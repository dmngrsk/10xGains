{
  "name": "@txg/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types (API DTOs, commands and database schema) for the 10xGains frontend and backend. Types only — no runtime code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "echo \"No tests in @txg/shared (types only)\""
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
