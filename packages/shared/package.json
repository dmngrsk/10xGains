{
  "name": "@txg/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types (API DTOs, commands, database schema and the domain union types for the 10xGains frontend and backend. Types only — no runtime code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "echo \"No tests in @txg/shared (types only)\""
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
