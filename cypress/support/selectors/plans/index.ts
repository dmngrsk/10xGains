import { dialogSelectors } from './dialog.selectors';
import { planEditSelectors } from './plan-edit.selectors';
import { planListSelectors } from './plan-list.selectors';

export const plansSelectors = {
  dialogs: dialogSelectors,
  planEdit: planEditSelectors,
  planList: planListSelectors,
};
